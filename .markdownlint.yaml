default: true

# Allow long lines — common in tables and links
MD013: false

# Allow HTML in markdown — used for centered badges in README
MD033: false

# Allow duplicate headings across documents
MD024:
  siblings_only: true

# Allow trailing punctuation in headings
MD026: false

# README starts with centered HTML, not a heading
MD041: false
